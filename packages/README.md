# Packages

This directory hosts the modular monolith boundaries described in
`docs/BHP Management Console Architecture.md` (Section 4).
Historical boundary notes live in `docs/archive/Epic 0 Module Boundaries.md`.

Each boundary should include a short `README.md` with ownership and scope when code
is added.
